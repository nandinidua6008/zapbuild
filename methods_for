a=(1 .. 10).to_a
puts "is 1 in range a? #{a.include?(1)}" #include() method is used to check whether the particular element is present in the range or not.
puts "minimum value in range is #{a.min()}"
puts "maximum value in range is #{a.max()}"
a.each{|a|print(a)}
